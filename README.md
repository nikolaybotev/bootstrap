# Bootstrap

Setup a new macOS, Linux, FreeBSD, or Cygwin user account for development in seconds.

```
sh -ec "$(curl -fsSL https://raw.github.com/nikolaybotev/bootstrap/main/install.sh)"
```

## Update

After installation, run:

```
~/.bootstrap/update
```
